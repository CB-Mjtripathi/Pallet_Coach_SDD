# Pallet Coach UI

Frontend for RF-005 React UI Home and Run Experience.

## Requirements
- Node.js 18+

## Development
- npm install
- npm run dev

Vite dev server proxies `/api` and `/output` to `http://localhost:8000`.

## Build
- npm run build

## Test
- npm run test
